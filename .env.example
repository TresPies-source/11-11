# =============================================================================
# The Workbench - Environment Variables Configuration
# =============================================================================
# Copy this file to `.env.local` and fill in your actual values.
# NEVER commit `.env.local` to version control.

# =============================================================================
# Development Mode
# =============================================================================

# Enable development mode (bypasses Google OAuth, uses mock data)
# Set to "true" for local development without Google Drive credentials
# Set to "false" or remove for production
NEXT_PUBLIC_DEV_MODE=true

# =============================================================================
# Google OAuth Configuration
# =============================================================================

# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
# Required for production Google Drive integration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
# Get from: https://console.cloud.google.com/apis/credentials
# Required for production Google Drive integration
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Google Drive Folder IDs
# =============================================================================

# Folder ID for Prompts directory (/03_Prompts)
# Get from: Google Drive folder URL (the ID after /folders/)
# Example: https://drive.google.com/drive/folders/ABC123XYZ -> ABC123XYZ
GOOGLE_DRIVE_PROMPTS_FOLDER_ID=your-prompts-folder-id

# Folder ID for PRDs directory (/01_PRDs)
# Get from: Google Drive folder URL (the ID after /folders/)
GOOGLE_DRIVE_PRDS_FOLDER_ID=your-prds-folder-id

# Public variants (exposed to client-side code)
# These should match the values above
NEXT_PUBLIC_DRIVE_PROMPTS_FOLDER_ID=your-prompts-folder-id
NEXT_PUBLIC_DRIVE_PRDS_FOLDER_ID=your-prds-folder-id

# =============================================================================
# NextAuth Configuration
# =============================================================================

# NextAuth Secret (used to encrypt session tokens)
# Generate with: openssl rand -base64 32
# Or online: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-random-secret-key-here

# NextAuth URL (your app's base URL)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# Development Mode Notes
# =============================================================================
# When NEXT_PUBLIC_DEV_MODE=true:
# - Google OAuth is bypassed (no login required)
# - Mock data is used for file tree
# - API calls return mock responses
# - Sync operations are simulated
#
# For production deployment:
# 1. Set NEXT_PUBLIC_DEV_MODE=false (or remove the variable)
# 2. Configure all Google OAuth credentials
# 3. Set up Google Drive folder IDs
# 4. Generate and set NEXTAUTH_SECRET
# 5. Update NEXTAUTH_URL to your production domain
#
# =============================================================================
# Database (PGlite)
# =============================================================================
# Database is stored in browser IndexedDB (idb://11-11-db)
# No configuration required - it's automatically created on first run
# Data persists in your browser's local storage

# =============================================================================
# LLM Configuration (Multi-Model Infrastructure)
# =============================================================================

# DeepSeek API Key (Primary LLM Provider)
# Get from: https://platform.deepseek.com/api_keys
# Used for: All agents (supervisor, librarian, cost-guard, dojo, debugger)
# Cost: $0.28/1M input tokens, $0.42/1M output tokens (90% cheaper with cache hits!)
# Models: deepseek-chat (general), deepseek-reasoner (complex reasoning)
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# OpenAI API Key (Fallback LLM Provider)
# Get from: https://platform.openai.com/api-keys
# Used for: Fallback when DeepSeek unavailable, embeddings (text-embedding-3-small)
# Optional: Dev mode works without API key (uses keyword-based fallback routing)
OPENAI_API_KEY=your-openai-api-key-here

# Model Selection (Optional Overrides)
# Override default model selection per agent if needed
# DEFAULT_MODEL=deepseek-chat
# REASONING_MODEL=deepseek-reasoner
# FALLBACK_MODEL=gpt-4o-mini
